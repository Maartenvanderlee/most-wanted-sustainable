node_modules/
.next/
.env.local
.env
.cache/
*.tsbuildinfo

# Lokale data-exports (scripts/export-archive.mjs, standaardmodus) — blijven lokaal.
# De duurzame kopie in data/backup/ (ARCHIVE_TARGET=committed) wordt JUIST wel
# getrackt: git-historie is daar het versiebeheerde backup buiten Supabase.
data/archive/
